import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export const metadata = { title: "Cart" };

export default function CartPage() {
  return <RoutePlaceholder eyebrow="CART" title="Cart boundary" description="Cart persistence and checkout adapters will be added after the catalog and commerce decisions are approved." />;
}
