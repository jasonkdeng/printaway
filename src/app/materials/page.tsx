import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export const metadata = { title: "Materials" };

export default function MaterialsPage() {
  return <RoutePlaceholder eyebrow="MATERIALS" title="Material reference" description="Material details belong to approved product data and are not represented by placeholder claims." />;
}
