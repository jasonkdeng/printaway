import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export const metadata = { title: "About" };

export default function AboutPage() {
  return <RoutePlaceholder eyebrow="ABOUT" title="About Printaway" description="Brand and production content will be added only when approved source material is available." />;
}
