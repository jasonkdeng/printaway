import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export const metadata = { title: "Shop" };

export default function ShopPage() {
  return <RoutePlaceholder eyebrow="SHOP" title="Small-batch printed objects" description="Catalog data is not configured yet. This route will expose material and availability filters only." />;
}
